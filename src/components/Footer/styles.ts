import styled from 'styled-components'

export const Container = styled.footer`
  position: absolute;
  bottom: 10px;
  left: auto;
  right: auto;

  a {
    font-weight: 500;
    text-decoration: none;
    color: inherit;
  }
`
